<%- include('partials/logged-in-header') -%>

<div class="container card-container">
  <div class="row justify-content-center mt-5">
    <div class="col-6">
      <h2><%= post.patientName %></h2>
      <img class="img-fluid" src="<%= post.image%>" />
      <div class="row justify-content-between">
        <!-- <form
          class="col-1"
          action="/post/likePost/<%= post.id %>?_method=PUT"
          method="POST"
        >
          <button class="btn btn-primary fa fa-heart" type="submit"></button>
        </form> -->
        <!-- <h3 class="col-3">Likes: <%= post.likes %></h3> -->
        <%if(post.user == user.id){ %>
        <form
          action="/post/deletePost/<%= post.id %>?_method=DELETE"
          method="POST"
          class="col-3"
        > 
          <button class="btn btn-primary fa fa-trash" type="submit"> Discharge</button>
        </form> 
        
        <%}%>
      </div>
    </div>
    <div class="col-3 mt-5">
      <p>Diagnosis: <%= post.diagnosis %></p>
      <p>Assist Levels: <%= post.caption %></p>
      <p>Goals: <%= post.ptGoals %></p>
      <p>Location: <%= post.locationAddress %></p>
      <p>Phone Number: <%= post.ptPhoneNum %></p>
      <p>Email: <%= post.ptEmail %></p>

    </div>
    <!-- <div class="col-6 mt-5">
      <a class="btn btn-primary" href="/profile">Return to Homepage</a>
      <a class="btn btn-primary" href="/feed">Return to Patient Census</a>
    </div> -->
  </div>
</div>

<%- include('partials/footer') -%>
